import "./navbar.css";

export const NavBar = () => {
  return (
    <nav>
      <title>TeeRex Store</title>
      <section>
        <span>Products</span>
        <span>Cart</span>
      </section>
    </nav>
  );
};
